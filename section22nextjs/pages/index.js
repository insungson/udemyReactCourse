// ourdomain.com/  로 접근하면 자동으로 연결된다.
// import React from 'react';  // 이건 NextJS 에서 빌트인 되어있기 때문에 굳이 import 할 필요가 없다!

function HomePage() {
  return <h1>The Home Page</h1>;
}

export default HomePage;
